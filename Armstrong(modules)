a1.py:
#First file takes the input from the user
def takeInput():
    n=int(input("Enter a number to check if it is armstrong number: "))
    return n
    
    
a2.py:from one import takeInput 
from two import armstrong
if armstrong(takeInput()):
    print("armstrong number")
else:
	print("not armstrong number")

#This file will have the main code:
import one 
def armstrong(num):
    length = len(str(num))
    sum = 0
    temp = num
    while(temp != 0):
	    sum = sum + ((temp % 10) ** length)
	    temp = temp // 10
    if sum==num:
        return True
    else:
        return False


a3.py:
#This file displays the output:from one import takeInput 
from two import armstrong
if armstrong(takeInput()):
    print("armstrong number")
else:
	print("not armstrong number")
